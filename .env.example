# AI Utilities Environment Configuration
# Copy this file to .env and fill in your actual values

# OpenAI API Key (required for OpenAI provider)
# Get your key from: https://platform.openai.com/api-keys
AI_API_KEY=your-key-here

# Optional: Override provider (default: openai)
# AI_PROVIDER=openai

# Optional: Override model (default: test-model-1)
# AI_MODEL=gpt-4

# Optional: Override temperature (default: 0.7)
# AI_TEMPERATURE=0.5

# Optional: Override base URL (required for openai_compatible)
# AI_BASE_URL=http://localhost:11434/v1
